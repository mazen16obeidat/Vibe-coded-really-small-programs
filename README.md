# Vibe-coded-really-small-programs
The name is self explanatory 

Most programs can run standalone without downloading anything other than the .exe if else it will be in a rar file.
some .exe are more than 25mb so they will also be in a rar file but in the commit it will be noted that it's a standalone program.
